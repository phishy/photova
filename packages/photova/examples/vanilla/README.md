# Brighten - Vanilla JS Example

A minimal example of using Brighten with plain HTML and JavaScript.

## Setup

```bash
npm install
npm run dev
```

Then open http://localhost:5173 in your browser.

## Usage

This example demonstrates:

- Loading the editor into a container
- Opening images from file input
- Exporting edited images
- Dark theme configuration
